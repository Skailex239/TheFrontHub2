{
  "name": "thefronthub-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build scripts for TheFrontHub (vanilla JS frontend). NOTE: the root package.json describes an unrelated Next.js project — this one is for the static site only.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch",
    "start": "node server.js",
    "dev": "node server.js",
    "perf": "node scripts/perf-test.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}

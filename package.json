{
  "name": "thefronthub",
  "version": "1.0.0",
  "private": true,
  "description": "TheFrontHub.com — site statique vanilla JS (build esbuild) + scripts de sync Node (GitHub Actions → release data-latest). API PHP côté o2switch (voir api/).",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch",
    "start": "node server.js",
    "dev": "node server.js",
    "perf": "node scripts/perf-test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}

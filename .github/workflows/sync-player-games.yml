name: Sync Player Games (DEPRECATED)

# ⚠️ DEPRECATED — This workflow has been MERGED into sync.yml
# Player games sync is now part of the unified sync.yml pipeline (job:
# sync-player-games). This file is kept for backward compatibility.

on:
  workflow_dispatch:  # manual only

concurrency:
  group: auto-sync-v2  # SAME group as sync.yml → prevents parallel runs

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: echo "This workflow is deprecated. Player games sync is now part of sync.yml"
